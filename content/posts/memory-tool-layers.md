---
title: '当我试图给 Agent 装记忆'
date: 2026-03-19T00:30:00-07:00
draft: false
slug: "memory-tool-layers"
tags: ["OpenClaw", "Memory", "Architecture", "Agent Design"]
---

## 遗忘

我的 agent 把我忘了。

不是隐喻。在一个话题里我们聊了很久一个部署方案，换到另一个话题继续的时候，它问我有什么可以帮忙的。它甚至没有那种忘记了什么的不安。它不是忘了，它是从来不知道自己应该记得。

于是我开始在社区里翻，想给它装上某种长期记忆。Nocturne、lossless-claw、QMD、ClawDB、Sirchmunk、MemOS——名字一个比一个陌生，README 一个比一个自信。每个项目都在说自己解决了记忆问题，但当我试图把它们放在一起比较的时候，发现比不了。有人说 Nocturne 比 ClawDB 好，有人说反过来，两边的论据都无懈可击，结论却完全相反。

我在这种矛盾里转了好几天，直到某个晚上突然想明白：不是答案矛盾。是我一直在拿螺丝刀和锤子比较谁更锋利。

## 一个词，三件事

问题出在**记忆**这两个字上。

它太大了。就像你说一个人性格好，到底是说他温柔、说他守信、还是说他在你搬家的时候会来帮忙？这三件事之间几乎没有交集，但它们都可以被叫做性格好。

在 agent 系统里也是一样。我们说记忆的时候，其实在同时谈论三件毫无关系的事情。

第一件是**身份**。agent 应该遵守什么原则、回避什么话题、在什么情况下用什么语气。这些东西不会随着对话变化，它们更像是性格，而不是经历。

第二件是**经历**。我们上周聊了什么，做了什么决定，在哪个问题上吵了半天最后选了方案 B。这些东西会随时间增长，会过期，会被后来的决定推翻。

第三件是**知识**。某个配置参数的默认值是什么，那篇笔记放在哪个文件夹里，那段代码是谁在什么时候改的。这些东西需要的不是回忆，是检索——带路径、带行号、带出处的那种。

这三件事需要完全不同的存储方式、调用时机、失败策略。但因为它们共享了同一个名字，它们被塞进了同一个抽屉。结果就是 RAG 开始冒充人格，摘要开始冒充事实，相似度匹配开始冒充原则。每一步看起来都说得过去，但整个系统慢慢变成了一团无法调试的雾。

## 一刀

调研中最花时间的部分，不是比较工具，而是理解对话轨迹这一层里面还藏着什么。

我原本以为对话记忆就是一件事：存下来，找回来。后来才发现这里面有两个机制，它们的成本结构完全相反，像同一个房间里住着两个互相讨厌的人。

一个是**上下文引擎**。它在每一轮对话里都要出场，负责决定 prompt 怎么拼、旧消息留多少、摘要做到什么粒度。它每动一下，prefix 缓存就碎一次，延迟和成本跟着涨。它是一个安静不下来的机制。

另一个是**记忆后端**。它只在被点名的时候才出场——agent 主动调了 `memory_search`，它才开始干活。不调就不花一分钱。它是一个可以沉默的机制。

把这两个混在一起的后果，我在自己的账单上见过。配了 lossless-claw 之后，token 用量翻了将近一倍，但 agent 并没有变得更聪明，它只是每轮都在重新翻看所有的旧对话，像一个考试前把整本书从头读一遍的学生。问题不是出在 lossless-claw 本身——它是一个很好的上下文引擎——而是出在我把它当成了记忆后端来用。

想通这一刀之后，分层就不再是一个需要设计的东西了。它是问题本身的形状。

## 四层

**L0** 管身份。原则、偏好、禁忌。它不检索任何东西，它只裁决：这件事该不该做，那个话题该不该碰。在所有行动之前，它先画出边界。

**L3** 管经历。拆成两半：L3a 是上下文引擎，每轮都在场，负责 prompt 的拼装和压缩；L3b 是记忆后端，按需出场，负责回答 agent 的主动查询。

**L2** 管本地知识。我的笔记里有没有这个，那篇文档里第几行写了那句话。它输出的不是答案，是证据——path、snippet、position。

**L1** 管外网。成本最高，不确定性最大，是最后的升级路径。

| 层 | 管什么 | 什么时候动 | 为什么这个顺序 |
| :--- | :--- | :--- | :--- |
| L0 | 身份 | 永远第一个 | 不知道自己是谁就不该开始找东西 |
| L3 | 经历 | 第二个 | 最便宜、最相关 |
| L2 | 本地知识 | 第三个 | 内部证据优先于外部 |
| L1 | 外网 | 最后 | 贵，而且不一定可信 |

编号和执行顺序是反的。L1 编号最小但最后执行，L3 编号最大但最先执行。编号是按信息的通用性排的——外网最通用，对话轨迹最私有。执行顺序是按成本排的——从最便宜的开始。这不矛盾，只是两套不同的坐标系。

## 各就各位

理解了分层之后，那些工具各自回到了自己该在的位置，像散场后观众终于找到了自己的座位。

**Nocturne** 是一个专门管 agent 人格的插件。它不做检索，它做的是存储和管理身份——你的 agent 遵守什么原则、回避什么话题、在什么条件下暴露什么信息。它带版本快照和回滚，也就是说你可以看到人格是什么时候被谁改过的，改错了还能退回去。这些事情听起来理所当然，但在 Nocturne 之前，大部分人的做法是把人格信息写在 system prompt 里，或者更糟，让 RAG 从向量数据库里检索出来。你的身份不应该取决于一次相似度匹配的结果。所以 Nocturne 只能是 **L0**。

**lossless-claw** 试图解决的是另一个痛点：对话太长之后，旧消息会被截断或丢弃。它的做法是把所有消息持久化存储，然后用一种叫 DAG 摘要的技术——简单说就是把对话历史组织成一棵树状结构，每个分支可以独立压缩——来决定当轮 prompt 里放什么。效果是旧的上下文不会被简单丢掉。但代价也很明确：它每轮都在参与 prompt 的拼装过程，这意味着每一轮发给模型的内容结构都可能不同，prefix 缓存因此很难复用，成本会上升。它是 **L3a**，一个上下文引擎。它不是你用来查旧对话的东西。

**memory-core** 是 OpenClaw 自带的默认记忆后端，用 Markdown 文件存储、SQLite 做索引，agent 通过 `memory_search` 和 `memory_get` 两个工具来查询。**QMD** 做的是同一件事，但后端换成了本地跑的搜索引擎，支持 BM25 关键词匹配、向量检索和 rerank（也就是先粗筛再精排）。**ClawDB** 走得更远，它是一个独立的服务化后端，用 WAL 日志保证写入可靠性，用 Parquet 列式存储来处理大量历史数据。这三个长得很不一样，但从 OpenClaw 的角度看，它们插在同一个槽位上——agent 调了 `memory_search`，谁来接？它们都是 **L3b**。在这个位置上可以互相比较，离开这个位置比较就没有意义。

**Sirchmunk** 做的事情和前面几个完全不同。它不是一个数据库，它是一个搜索 agent——给它一个问题，它会自己改写查询、采样证据、做摘要、重新排序，然后给你一个带出处的回答。本质上是把检索过程变成了一连串 LLM 驱动的决策。这意味着每次搜索都会产生 LLM 调用（哪怕是它的快速模式也不例外），成本比纯数据库查询高得多。如果你的本地文档量很大，而且你需要的不只是关键词匹配而是理解性的检索，Sirchmunk 就很有价值。它是 **L2**，本地知识层。

**MemOS Cloud Plugin** 的思路又不一样。它在每轮对话开始前自动去云端查一遍 agent 的历史记忆，把相关的片段注入到当轮 prompt 里；对话结束后再把新产生的信息写回去。对陪伴型 AI 来说这个策略完全合理——你希望 AI 每次醒来都记得你是谁、你们聊过什么。但如果你想要一个安静的、可预测的系统，那每轮自动注入就是在每轮增加噪声和成本。

归个位：

- **L0 身份** — Nocturne（人格管理、版本审计）
- **L3a 上下文引擎** — lossless-claw（DAG 摘要、prompt 拼装）
- **L3b 记忆后端** — memory-core / QMD / ClawDB（`memory_search` 的实际执行者）
- **L2 本地知识** — Sirchmunk（LLM 驱动的文档搜索）
- **L1 外网** — tavily 等外网检索工具

## 克制

调研做到最后，我没有选出一个最强的工具。我选出了一种审美。

好的记忆系统是安静的。它不会每轮都自动拉历史、塞 recall block、改写上下文。大多数时候 agent 不需要回忆任何事情，它只需要把眼前的活干完。

好的记忆系统是可解释的。每一次 recall 都应该能交代清楚——为什么现在查，查到了什么，证据在哪。交代不清楚的 recall 就不该发生。

好的记忆系统是 cache-friendly 的。每轮都重写 prompt 结构的系统天然更贵、更慢、更难调试。这不是优化问题，是架构问题。

好的记忆系统是可审计的。尤其是 L0。约束是谁写进去的，什么时候改过，能不能回滚。如果人格层不可审计，你就永远不知道 agent 为什么突然换了一种口气跟你说话。

最后落地的默认策略反而是最简单的那个：L0 画边界，L3b 被叫到的时候才出场，L2 需要文档证据的时候才出场，L1 实在找不到了再去外网。大部分时候，L0 加按需的 L3b 就够了。

如果你现在正站在我当初的位置上，面对一堆工具不知道从哪下手，我的建议是先想清楚你最痛的是哪一件事。agent 动不动变了语气、忘了你定下的规矩，那是 L0 的问题，先装 Nocturne。你只是需要它在跨会话的时候还记得之前聊过什么，那是 L3b 的问题，默认的 memory-core 可能就够用了，不需要急着换。你有大量本地文档要搜、而且关键词匹配已经不够用了，那再看 Sirchmunk。不要一次全装。每多一层都是多一个需要理解和维护的活动部件。

## 后来

写完这篇回过头想，记忆可能只是一个缩影。

在 agent 生态里，很多混乱看起来是技术选型的问题——该用哪个数据库、该选哪个框架、该不该上 RAG。但拆到底下去看，经常是同一个词在不同人嘴里指向了完全不同的东西，而大家在这个模糊的基础上争论、比较、做决策。

我现在觉得，在动手之前，最值得花时间的事情是把问题拆干净。搞清楚你到底在问几个问题，再去看每个工具回答的是哪一个。顺序反过来的话，你越研究越糊涂，因为你在一个不存在的维度上积累信息。

层位摆正，工具自然互补。层位混乱，再强的工具也只会互相污染。这件事大概不只适用于记忆。

---

## 参考

- [Nocturne Memory](https://github.com/Dataojitori/nocturne_memory) — L0 主权记忆
- [lossless-claw](https://github.com/Martian-Engineering/lossless-claw) — L3a 上下文引擎
- [QMD](https://github.com/tobi/qmd) — L3b 记忆后端
- [ClawDB](https://github.com/clawdb-ai/clawdb) — L3b 记忆后端
- [Sirchmunk](https://github.com/modelscope/sirchmunk) — L2 本地知识检索
